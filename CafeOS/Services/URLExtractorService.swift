import Foundation

// URLExtractorService has been superseded by ExtractionPipeline.
// Fetch logic (direct GET + Jina fallback + JSON envelope unwrapping) now lives in
// ExtractionPipeline.fetchRaw(url:) — see CafeOS/Services/ExtractionPipeline.swift.
// This file is intentionally empty and kept only to preserve git history.
